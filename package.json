{
  "name": "zyn-tracker",
  "version": "1.0.0",
  "description": "Simple web app for tracking Zyn pouch usage with analytics and export",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
